processResources {
    from(rootProject.getRootDir().toString() + "/sql-scripts") {
        include "geo.sql"
    }
}

dependencies {
    compile project(":core")
    compile group: 'com.uber', name: 'h3', version: uber_h3_version
}