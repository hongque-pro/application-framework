processResources {
    from(rootProject.getRootDir().toString() + "/sql-scripts") {
        include "auth.sql"
    }
}
dependencies {
    compile project(":core")
//    compile project(":commons-data")
}